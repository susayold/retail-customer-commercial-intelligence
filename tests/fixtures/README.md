# Synthetic fixture policy

The public repository may contain small synthetic fixtures for metric-contract tests. They are not drawn from the dunnhumby files and must never be presented as real findings. Real source data remains in Drive only.

The product fixture deliberately uses production-style `Private`/`National` values plus case and whitespace variants. Staging must normalize these values to the governed `PRIVATE`/`NATIONAL` domain without changing the raw source.
